export interface AngularPluginInfo {
  name: string;
  version: string;
  description: string;
  // Define other properties that will eventually be sent by the backend
  // routePath?: string;
  // modulePath?: string;
  // moduleName?: string;
  // navigationLabel?: string;
  // requiredRole?: string;
}
